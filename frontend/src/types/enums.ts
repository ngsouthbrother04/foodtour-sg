// Enum cho sort field
export enum SortField {
  NAME = 'name',
  PRICE = 'price',
  DISTRICT = 'district',
}

// Enum cho sort order
export enum SortOrder {
  ASC = 'asc',
  DESC = 'desc',
}

// Enum cho nguồn dữ liệu
export enum DataSource {
  FOODTOUR = 'foodtour',
  SAIGON_EVERYFOOD = 'saigon_everyfood',
}
